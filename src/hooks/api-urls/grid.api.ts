import { adminApiPrefix } from "../../constants";

export const gridApis = {
  GET_GRIDS: adminApiPrefix + "get-grids",
  CREATE_GRID: adminApiPrefix + "create-grid",
  INACTIVE_GRID: adminApiPrefix + "inactive-grid",
  UPLOAD_GRIDS: adminApiPrefix + "upload-grids",
};
