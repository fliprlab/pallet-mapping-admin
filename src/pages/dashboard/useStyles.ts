import { createStyles } from "@mantine/core";
export const styles = createStyles({
  root: {
    padding: "20px 52px",
    background: "linear-gradient(180deg, #3735AF 0%, #872DE8 100%)",
  },
  heading: {
    fontSize: 20,
    fontWeight: 700,
    marginTop: 0,

    color: "#fff",
  },
  para: {
    fontSize: 12,
    fontWeight: 500,
    color: "#E7E7E7",
    margin: 0,
  },
});
